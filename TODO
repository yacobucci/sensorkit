- make a routes module
  - individual measurement endpoints based on discovered meters
  - API endpoints based on discovered meters

- make route Response configurable
  - prometheus
  - json

- wire up a detector
  - server sent events or websockets for notification?
